[pytest]
markers =
    unit: fast tests with no real external dependency.
    integration: tests that exercise replaceable infrastructure such as Chroma, MySQL-compatible paths, or mock external providers.
    release: release smoke tests that require a deployed app or sandbox/real external services.
